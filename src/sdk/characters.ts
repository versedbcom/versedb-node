/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { charactersGetCharacterDetails } from "../funcs/characters-get-character-details.js";
import { charactersGetCharacterIssues } from "../funcs/characters-get-character-issues.js";
import { charactersGetCharacterSeries } from "../funcs/characters-get-character-series.js";
import { charactersGetCharacterTeams } from "../funcs/characters-get-character-teams.js";
import { charactersListCharacters } from "../funcs/characters-list-characters.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Characters extends ClientSDK {
  /**
   * List characters.
   *
   * @remarks
   * Returns paginated characters with optional search and sorting.
   */
  async listCharacters(
    request?: operations.ListCharactersRequest | undefined,
    options?: RequestOptions,
  ): Promise<operations.ListCharactersResponse> {
    return unwrapAsync(charactersListCharacters(
      this,
      request,
      options,
    ));
  }

  /**
   * Get character details.
   *
   * @remarks
   * Returns a single character with full details including publishers and
   * cached relationship counts (appearances, series, teams, story arcs) so
   * clients can decide which related-entity tabs to expose.
   */
  async getCharacterDetails(
    request: operations.GetCharacterDetailsRequest,
    options?: RequestOptions,
  ): Promise<operations.GetCharacterDetailsResponse> {
    return unwrapAsync(charactersGetCharacterDetails(
      this,
      request,
      options,
    ));
  }

  /**
   * Get character series.
   *
   * @remarks
   * Returns paginated series where the character appears.
   *
   * Requires PRO subscription on the User API (`/api/...`). Returns 402 Payment Required for non-PRO users on that prefix.
   * The Mobile API (`/mobile/api/...`) is unrestricted — its route group does not apply the `pro.api` middleware.
   */
  async getCharacterSeries(
    request: operations.GetCharacterSeriesRequest,
    options?: RequestOptions,
  ): Promise<operations.GetCharacterSeriesResponse> {
    return unwrapAsync(charactersGetCharacterSeries(
      this,
      request,
      options,
    ));
  }

  /**
   * Get character issues.
   *
   * @remarks
   * Returns paginated issues where the character appears, ordered by release date.
   *
   * Requires PRO subscription on the User API (`/api/...`). Returns 402 Payment Required for non-PRO users on that prefix.
   * The Mobile API (`/mobile/api/...`) is unrestricted — its route group does not apply the `pro.api` middleware.
   */
  async getCharacterIssues(
    request: operations.GetCharacterIssuesRequest,
    options?: RequestOptions,
  ): Promise<operations.GetCharacterIssuesResponse> {
    return unwrapAsync(charactersGetCharacterIssues(
      this,
      request,
      options,
    ));
  }

  /**
   * Get character teams.
   *
   * @remarks
   * Returns paginated teams the character is a member of, including membership details.
   *
   * Requires PRO subscription on the User API (`/api/...`). Returns 402 Payment Required for non-PRO users on that prefix.
   * The Mobile API (`/mobile/api/...`) is unrestricted — its route group does not apply the `pro.api` middleware.
   */
  async getCharacterTeams(
    request: operations.GetCharacterTeamsRequest,
    options?: RequestOptions,
  ): Promise<operations.GetCharacterTeamsResponse> {
    return unwrapAsync(charactersGetCharacterTeams(
      this,
      request,
      options,
    ));
  }
}
