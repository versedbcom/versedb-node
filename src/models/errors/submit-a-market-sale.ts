/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import * as types from "../../types/primitives.js";
import { VerseDbError } from "./verse-db-error.js";

/**
 * Rate limit exceeded. This endpoint allows 150/hour (free) or 500/hour (PRO). Wait for the number of seconds in the `Retry-After` header before retrying.
 */
export type SubmitAMarketSaleTooManyRequestsErrorData = {
  message: string;
};

/**
 * Rate limit exceeded. This endpoint allows 150/hour (free) or 500/hour (PRO). Wait for the number of seconds in the `Retry-After` header before retrying.
 */
export class SubmitAMarketSaleTooManyRequestsError extends VerseDbError {
  /** The original data that was passed to this error instance. */
  data$: SubmitAMarketSaleTooManyRequestsErrorData;

  constructor(
    err: SubmitAMarketSaleTooManyRequestsErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;

    this.name = "SubmitAMarketSaleTooManyRequestsError";
  }
}

/**
 * Duplicate
 */
export type SubmitAMarketSaleConflictErrorData = {
  message?: string | undefined;
  existingId?: number | undefined;
  status?: string | undefined;
};

/**
 * Duplicate
 */
export class SubmitAMarketSaleConflictError extends VerseDbError {
  existingId?: number | undefined;
  status?: string | undefined;

  /** The original data that was passed to this error instance. */
  data$: SubmitAMarketSaleConflictErrorData;

  constructor(
    err: SubmitAMarketSaleConflictErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    if (err.existingId != null) this.existingId = err.existingId;
    if (err.status != null) this.status = err.status;

    this.name = "SubmitAMarketSaleConflictError";
  }
}

/**
 * Unauthenticated. The bearer token is missing, invalid, or revoked.
 */
export type SubmitAMarketSaleUnauthorizedErrorData = {
  message: string;
};

/**
 * Unauthenticated. The bearer token is missing, invalid, or revoked.
 */
export class SubmitAMarketSaleUnauthorizedError extends VerseDbError {
  /** The original data that was passed to this error instance. */
  data$: SubmitAMarketSaleUnauthorizedErrorData;

  constructor(
    err: SubmitAMarketSaleUnauthorizedErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;

    this.name = "SubmitAMarketSaleUnauthorizedError";
  }
}

/** @internal */
export const SubmitAMarketSaleTooManyRequestsError$inboundSchema: z.ZodMiniType<
  SubmitAMarketSaleTooManyRequestsError,
  unknown
> = z.pipe(
  z.object({
    message: types.string(),
    request$: z.custom<Request>(x => x instanceof Request),
    response$: z.custom<Response>(x => x instanceof Response),
    body$: z.string(),
  }),
  z.transform((v) => {
    return new SubmitAMarketSaleTooManyRequestsError(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  }),
);

/** @internal */
export const SubmitAMarketSaleConflictError$inboundSchema: z.ZodMiniType<
  SubmitAMarketSaleConflictError,
  unknown
> = z.pipe(
  z.object({
    message: types.optional(types.string()),
    existing_id: types.optional(types.number()),
    status: types.optional(types.string()),
    request$: z.custom<Request>(x => x instanceof Request),
    response$: z.custom<Response>(x => x instanceof Response),
    body$: z.string(),
  }),
  z.transform((v) => {
    const remapped = remap$(v, {
      "existing_id": "existingId",
    });

    return new SubmitAMarketSaleConflictError(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  }),
);

/** @internal */
export const SubmitAMarketSaleUnauthorizedError$inboundSchema: z.ZodMiniType<
  SubmitAMarketSaleUnauthorizedError,
  unknown
> = z.pipe(
  z.object({
    message: types.string(),
    request$: z.custom<Request>(x => x instanceof Request),
    response$: z.custom<Response>(x => x instanceof Response),
    body$: z.string(),
  }),
  z.transform((v) => {
    return new SubmitAMarketSaleUnauthorizedError(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  }),
);
